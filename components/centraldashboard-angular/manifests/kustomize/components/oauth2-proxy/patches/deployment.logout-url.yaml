apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-angular
spec:
  template:
    spec:
      containers:
        - name: dashboard-angular
          env:
            - name: LOGOUT_URL
              value: /oauth2/sign_out
