apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeflow

resources:
- ../../base
- httproute.yaml

components:
- ../../components/remove-system-namespace
- ../../components/kfam

commonLabels:
  kustomize.component: profiles

patchesStrategicMerge:
- patches/manager.yaml
