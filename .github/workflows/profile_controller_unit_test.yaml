name: Run Profile Controller unit tests
on:
  pull_request:
    paths:
      - components/profile-controller/**
      ## NOTE: we also test on changes to access-management as access-management depends on profile-controller
      - components/access-management/**
      - releasing/version/VERSION
    branches:
      - main
      - v*-branch
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

    - name: Install Go
      uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
      with:
        go-version-file: components/profile-controller/go.mod
        check-latest: true
        cache-dependency-path: components/profile-controller/go.sum

    - name: Build
      working-directory: components/profile-controller
      run: make build

    - name: Run unit tests
      working-directory: components/profile-controller
      run: make test

    - name: Check if there are uncommitted file changes
      working-directory: components/profile-controller
      run: |
        clean=$(git status --porcelain)
        if [[ -z "$clean" ]]; then
            echo "Empty git status --porcelain: $clean"
        else
            echo "Uncommitted file changes detected: $clean"
            git diff
            exit 1
        fi

    - name: Check if there are uncommitted file changes
      working-directory: components/profile-controller
      run: |
        clean=$(git status --porcelain)
        if [[ -z "$clean" ]]; then
            echo "Empty git status --porcelain: $clean"
        else
            echo "Uncommitted file changes detected: $clean"
            git diff
            exit 1
        fi
